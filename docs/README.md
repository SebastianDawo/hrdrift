# docs/

`index.html` is the drop-a-file analyser, served by GitHub Pages. It parses and
analyses an activity entirely in the browser.


```bash
hrdrift --webapp docs/index.html
```

CI fails if it is stale.
